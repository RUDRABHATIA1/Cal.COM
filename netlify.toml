[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  # This makes React Router work correctly on Netlify
  from = "/*"
  to = "/index.html"
  status = 200
